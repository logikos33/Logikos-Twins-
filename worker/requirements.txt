# Dependências do worker de produção (GPU).
#
# Licenças verificadas em LICENSES.md. Regra dura: nada de AGPL ou copyleft forte —
# o que bane o stack ultralytics. Ver ADR-0005.
#
# torch e o próprio LingBot-Map são instalados no Dockerfile (índice CUDA específico
# e clone com commit pinado), não aqui. O motor entra com `pip install --no-deps`:
# o pyproject dele declara `opencv-python` (NÃO-headless), que conflita com o nosso
# headless — por isso as dependências DELE são declaradas explicitamente abaixo.

runpod==1.7.9
boto3==1.35.99
numpy==1.26.4
opencv-python-headless==4.10.0.84
onnxruntime==1.20.1

# --- deps do lingbot-map (pyproject do pin, sem o extra [vis]/[demo]) ---------
# opencv já coberto pelo headless acima; numpy<2 é exigência do motor (README).
Pillow==11.1.0
huggingface_hub==0.28.1
einops==0.8.0
safetensors==0.5.2
scipy==1.15.1
tqdm==4.67.1
