# Ferramental de desenvolvimento e teste, para rodar o pipeline em CPU sobre a
# cena sintética. Sem torch, sem CUDA — o motor de verdade só roda no plug-in.
-r requirements.txt

pytest==8.3.4
mypy==1.14.1
ruff==0.9.2
