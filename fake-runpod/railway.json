{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile" },
  "deploy": {
    "startCommand": "uvicorn app:app --host :: --port 8080",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60
  }
}
